<project default="all" >

    <property name="sourceRepository" value="https://github.com/Wanderer19/First.git" />
    <property name="projectDirectory" value="First"/>
    <property name="branchName" value="master"/>
    <property name="sources" value="sources"/>
    <property name="maven.home" value="C:\Program Files\Appache\apache-maven-3.2.5"/>
    <property name="maven.options" value="-DskipTests=true --update-snapshots -DrepoUrl=http://vm-maven:8081/nexus/content/groups/public/"/>
    <property name="archives" value="archives"/>
    <property name="localRepository" value="C:\Users\wanderer\.m2\repository"/>

    <target name="all">
        <antcall target="clean-all"/>
        <antcall target="creating"/>
        <antcall target="git-cloning"/>
        <antcall target="building"/>
        <antcall target="archiving"/>
        <antcall target="delete_sources"/>
    </target>

    <target name="clean-all">
        <delete dir="${sources}"/>
        <delete dir="${archives}"/>
        <delete dir="${localRepository}"/>
    </target>

    <target name="creating">
        <mkdir dir="${sources}"/>
        <mkdir dir="${archives}"/>
        <mkdir dir="${localRepository}"/>
    </target>

    <target name="git-cloning">
        <exec executable="cmd">
            <arg value="/c"/>
            <arg value="git-cloning.bat"/>
            <arg value="-p"/>
            <arg value="${sources}"/>
            <arg value="${sourceRepository}"/>
            <arg value="${projectDirectory}"/>
            <arg value="${branchName}"/>
        </exec>
    </target>


    <macrodef name="maven">
        <attribute name="options" default="" />
        <attribute name="goal" />
        <attribute name="basedir" />
        <attribute name="resultproperty" default="maven.result" />
        <element name="args" implicit="true" optional="true" />
        <sequential>
            <java classname="org.codehaus.classworlds.Launcher" fork="true"
                  dir="@{basedir}" resultproperty="@{resultproperty}">
                <jvmarg value="-Xmx512m"/>
                <classpath>
                    <fileset dir="${maven.home}/boot">
                        <include name="*.jar" />
                    </fileset>
                    <fileset dir="${maven.home}/lib">
                        <include name="*.jar" />
                    </fileset>
                </classpath>
                <sysproperty key="classworlds.conf" value="${maven.home}/bin/m2.conf" />
                <sysproperty key="maven.home" value="${maven.home}" />
                <arg line="--batch-mode @{options} @{goal}" />
            </java>
        </sequential>
    </macrodef>

    <target name="building">
        <maven basedir="${sources}/${projectDirectory}"
               goal="clean install"
               options="${maven.options}"
               resultproperty="maven.build.result"/>
    </target>

    <target name="archiving">

        <zip destfile="${archives}/project.zip"  basedir="${sources}/${projectDirectory}/target"
             includes="*.jar, *.war, *.ear"
             update="true"/>

        <zip destfile="${archives}/libs.zip"  basedir="${localRepository}"
             update="true"/>
    </target>

    <target name="delete_sources">
        <delete dir="${sources}"/>
    </target>

</project>